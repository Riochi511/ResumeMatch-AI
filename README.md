---
title: NLP Job Matcher
emoji: 💼
colorFrom: blue
colorTo: green
sdk: docker
pinned: false
---

# NLP Job Matcher

Match your resume to job postings using TF-IDF and cosine similarity.

POST to `/match` with either `resume_text` or a `resume_file` (PDF/DOCX).
